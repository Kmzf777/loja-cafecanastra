export default function Home() {
  // Placeholder. O design da home e de um plano futuro — as utilidades abaixo
  // existem so para comprovar que o pipeline do Tailwind esta ativo na vitrine.
  return (
    <main className="p-8 text-2xl font-semibold">
      Vitrine Café Canastra — em construção.
    </main>
  );
}
