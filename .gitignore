node_modules
.env
dist
uploads
.DS_Store
estrutura.sql
.idea
# Artefato criado por engano ao rodar next fora de frontend/
.next

# Dumps CSV do banco da loja antiga (Shopnaw). Contem dado pessoal real —
# nome, e-mail, telefone, endereco, hash de senha e refresh token de terceiros.
# Nao versionar. Ver docs/seguranca-dados-pessoais.md.
*.csv
.dumps-antigos/

# Variantes de env (.env.local, .env.production...). O padrao ".env" acima
# casa so o nome exato, e um .env.local com credencial iria para o repositorio.
.env*
!.env.example
